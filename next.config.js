/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["tailwindui.com", "ui-avatars.com", "images.unsplash.com"],
  } 
}

module.exports = nextConfig
